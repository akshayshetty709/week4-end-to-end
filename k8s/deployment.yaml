apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-app
spec:
  replicas: 2

  selector:
    matchLabels:
      app: node

  template:
    metadata:
      labels:
        app: node

    spec:
      tolerations:
      - key: "eks.amazonaws.com/compute-class"
        operator: "Equal"
        value: "general-purpose"
        effect: "NoSchedule"
      containers:
      - name: node-container
        image: 751101364131.dkr.ecr.ap-south-1.amazonaws.com/node-app:latest
        ports:
        - containerPort: 3000
     
